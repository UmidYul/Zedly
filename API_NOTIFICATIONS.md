# API Документация - Уведомления и Импорт

## 🔔 Система уведомлений

### Настройка

Добавьте в `.env` файл:

```env
# Email конфигурация
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password

# Telegram конфигурация
TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
TELEGRAM_CHAT_ID=your_default_chat_id
TELEGRAM_SECURITY_CHAT_ID=your_security_chat_id
TELEGRAM_OPERATIONS_CHAT_ID=your_operations_chat_id

# URL приложения (для ссылок в письмах)
APP_URL=http://localhost:3000
```

### Типы уведомлений

#### 1. Приветственное сообщение новому пользователю
Отправляется автоматически при создании пользователя через:
- `POST /api/admin/users`
- `POST /api/superadmin/schools/:schoolId/admins`
- `POST /api/admin/users/import`

**Содержит:**
- Логин и временный пароль
- Ссылку на страницу входа
- Инструкции по первому входу

#### 2. Уведомление о сбросе пароля
Отправляется при сбросе пароля через:
- `POST /api/admin/users/:id/reset-password`
- `POST /api/superadmin/schools/:schoolId/admins/:id/reset-password`

**Содержит:**
- Новый временный пароль
- Предупреждение о необходимости смены пароля
- Ссылку на страницу входа

#### 3. Уведомление о новом тесте
Отправляется студентам при назначении теста классу:
- `POST /api/teacher/assignments`

**Содержит:**
- Название теста и предмета
- Информацию о времени прохождения
- Ссылку на страницу теста

### Языковая поддержка

Все уведомления поддерживают два языка: **Русский (ru)** и **Узбекский (uz)**

Язык можно указать через query параметр:
```
POST /api/admin/users?lang=uz
```

По умолчанию используется русский язык.

### Маршрутизация Telegram по типу событий

- `reset_password`, `delete` → `TELEGRAM_SECURITY_CHAT_ID` (или fallback `TELEGRAM_CHAT_ID`)
- `create`, `update`, `import` → `TELEGRAM_OPERATIONS_CHAT_ID` (или fallback `TELEGRAM_CHAT_ID`)

---

## 📊 Импорт пользователей из Excel

### Endpoints

#### 1. Скачать шаблон для импорта
```
GET /api/admin/users/template
```

**Описание:** Загружает Excel файл с примерами и инструкциями

**Структура шаблона:**
| Поле | Обязательно | Описание |
|------|------------|----------|
| role | Да | student, teacher, или school_admin |
| first_name | Да | Имя пользователя |
| last_name | Да | Фамилия пользователя |
| email | Нет | Email для уведомлений |
| phone | Нет | Номер телефона |
| telegram_id | Нет | Telegram ID (@username или chat_id) |
| class_name | Нет | Название класса (только для студентов) |

#### 2. Импорт пользователей
```
POST /api/admin/users/import
Content-Type: multipart/form-data
```

**Параметры:**
- `file` (file) - Excel файл (.xlsx)
- `lang` (query, optional) - Язык уведомлений (ru/uz)

**Пример запроса с curl:**
```bash
curl -X POST http://localhost:5000/api/admin/users/import?lang=ru \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -F "file=@users.xlsx"
```

**Пример ответа:**
```json
{
  "message": "Import completed",
  "results": {
    "total": 10,
    "success": 8,
    "errors": 2,
    "details": {
      "success": [
        {
          "row": 2,
          "username": "ivani",
          "password": "ABC12XYZ",
          "name": "Иван Иванов"
        }
      ],
      "errors": [
        {
          "row": 5,
          "error": "Missing required fields (role, first_name, last_name)"
        }
      ]
    }
  }
}
```

**Что происходит при импорте:**
1. Автоматическая генерация уникальных логинов
2. Генерация OTP паролей (8 символов)
3. Добавление студентов в указанные классы
4. Отправка приветственных уведомлений
5. Логирование всех действий

#### 3. Экспорт пользователей
```
GET /api/admin/users/export?role=student
```

**Query параметры:**
- `role` (optional) - Фильтр по роли (student, teacher, school_admin, all)

**Описание:** Экспортирует всех пользователей школы в Excel файл

**Содержит поля:**
- role, username, first_name, last_name
- email, phone, telegram_id
- is_active, created_at, last_login
- classes (список классов для студента)

---

## 🔐 Безопасность

### Защита файлов
- Максимальный размер файла: **5MB**
- Разрешенные форматы: **только .xlsx**
- Файлы обрабатываются в памяти (не сохраняются на диск)

### Изоляция данных
- School Admin может импортировать пользователей только в свою школу
- Все пользователи автоматически привязываются к школе администратора
- Проверка доступа на уровне middleware

### Audit логи
Все операции импорта/экспорта логируются:
```sql
INSERT INTO audit_logs (user_id, action, entity_type, entity_id, details)
VALUES (admin_id, 'create', 'user', user_id, {source: 'excel_import'})
```

---

## 📝 Примеры использования

### Пример 1: Массовое добавление студентов

1. Скачайте шаблон Excel:
```bash
curl -O -J http://localhost:5000/api/admin/users/template \
  -H "Authorization: Bearer YOUR_TOKEN"
```

2. Заполните файл данными студентов:
```
| role    | first_name | last_name | email              | class_name |
|---------|------------|-----------|-------------------|-----------|
| student | Алия       | Камалова  | aliya@school.uz   | 9A        |
| student | Бехзод     | Азимов    | behzod@school.uz  | 9A        |
| student | Гулнора    | Рахимова  | gulnora@school.uz | 9B        |
```

3. Импортируйте файл:
```bash
curl -X POST http://localhost:5000/api/admin/users/import?lang=uz \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -F "file=@students.xlsx"
```

4. Получите результат с паролями для каждого студента

### Пример 2: Создание школьного администратора с уведомлением

```bash
curl -X POST http://localhost:5000/api/superadmin/schools/1/admins?lang=ru \
  -H "Authorization: Bearer SUPERADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "director_school1",
    "first_name": "Азамат",
    "last_name": "Каримов",
    "email": "director@school1.uz",
    "telegram_id": "@director_karimov",
    "phone": "+998901234567"
  }'
```

Администратор получит email и Telegram сообщение с временным паролем.

### Пример 3: Назначение теста с уведомлением

```bash
curl -X POST http://localhost:5000/api/teacher/assignments?lang=uz \
  -H "Authorization: Bearer TEACHER_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "test_id": 42,
    "class_id": 5,
    "start_date": "2026-02-12T08:00:00Z",
    "end_date": "2026-02-15T18:00:00Z"
  }'
```

Все студенты класса получат уведомление о новом доступном тесте.

---

## 🛠️ Устранение неполадок

### Email не отправляются
1. Проверьте настройки SMTP в `.env`
2. Для Gmail: используйте App Password вместо обычного пароля
3. Проверьте логи: `console` покажет ошибки отправки

### Telegram не работает
1. Убедитесь что `TELEGRAM_BOT_TOKEN` правильный
2. Пользователи должны сначала начать диалог с ботом
3. Используйте username (@user) или numeric chat_id

### Импорт не распознает файл
1. Убедитесь что файл формата `.xlsx` (не `.xls`)
2. Проверьте что первая строка - это заголовки
3. Убедитесь что обязательные поля заполнены

---

## 🔄 Workflow рекомендации

### Начало учебного года

1. **SuperAdmin создает школу и директора**
   - Директор получает email/Telegram с доступом

2. **Директор добавляет предметы**
   - Через веб-интерфейс или API

3. **Директор создает классы**
   - Назначает классных руководителей

4. **Директор импортирует учителей**
   - Excel файл со всеми учителями
   - Каждый получает уведомление

5. **Директор импортирует студентов**
   - Excel файлы по классам
   - Автоматическое добавление в классы
   - Все получают доступы

6. **Директор назначает учителей к классам и предметам**
   - Через интерфейс управления

7. **Учителя создают и назначают тесты**
   - Студенты получают уведомления

### Добавление новых пользователей в течение года

- **Единичное добавление:** через веб-интерфейс
- **Массовое добавление:** через Excel импорт
- **Уведомления:** отправляются автоматически

---

## 📚 Связанные документы

- [API_DOCS.md](./API_DOCS.md) - Полная документация API
- [QUICKSTART.md](./QUICKSTART.md) - Быстрый старт
- [README.md](./README.md) - Общая информация о проекте
